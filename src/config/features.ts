/**
 * Feature flag configuration for toggling extra features.
 * Set flags to `true` to enable full feature set.
 */
export const FEATURES = {
  SHOW_DAILY_CHALLENGE: true,
  SHOW_BADGE_SYSTEM: true,
} as const
